function createProjects(input){
    let name = input.shift();
    let progectCount = Number(input.shift());
    console.log(`The architect ${name} will  need ${projectCount*3} hours to complete ${progectCount} projects.`)
}
createProjects(["George","4"])